name: density_field_properties
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14

  # Scientific computing stack
  - numpy>=1.7
  - scipy>=1.7
  - matplotlib>=3.5

  # Numba for high-performance computing (CPU only)
  - numba

  - make
  - git

  # Data handling and I/O
  - h5py
  - pandas
  - pyarrow
  - scikit-learn

  #- typer
  #- loguru
  #- tqdm
  - ruff
  - pytest

  - pip
  - pip:
    - pre-commit>=4.6.0
    - identify>=2.6.0
    #- python-dotenv
    - mkdocs
    - numpy>=1.7
    - scipy>=1.7
    - matplotlib>=3.5
    - h5py>=3.15.1
    - bigfile>=0.1.52
